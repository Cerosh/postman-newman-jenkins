{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "project for storing postman tests first time",
  
  "directories": {
    "test": "test"
  },
  "scripts": {
    "api-tests-production": "newman run test/PMLearning.postman_collection.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Cerosh",

 "dependencies": {
    "newman": "^3.9.3"
  }
}
